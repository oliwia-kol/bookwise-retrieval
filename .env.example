# Backend API URL
#
# For local development with venv:
#   1. python -m venv .venv && source .venv/bin/activate
#   2. pip install -r requirements.txt
#   3. uvicorn api_server:app --reload --host 0.0.0.0 --port 8000
# Then use: http://localhost:8000
#
# For Codespaces: use your forwarded URL
# Example: https://your-codespace-abc123.app.github.dev

VITE_API_URL=http://localhost:8000

# Optional: set to "true" to use mock data instead of the real API
VITE_USE_MOCKS=false
